Task 8 is backend-focused (middleware, jobs, caching). No dedicated frontend is
required by the task brief; the `/api/popular-books` endpoint can be consumed
by the Task-5/6 React dashboard if you want to visualize cache-hit vs
cache-miss behavior.
