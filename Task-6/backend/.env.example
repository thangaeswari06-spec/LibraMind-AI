# Copy to .env and fill in for production (Supabase Postgres).
# If left blank, the server automatically falls back to a local SQLite
# file (dev.sqlite) so the project runs with zero external setup.
SUPABASE_URL=SUPABASE_URL
SUPABASE_SERVICE_KEY=SUPABASE_SERVICE_KEY
JWT_SECRET=libramind_super_secret_change_me
PORT=5006
